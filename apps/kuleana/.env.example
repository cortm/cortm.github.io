# Supabase — required for sync across devices (optional for local-only dev)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Shared household id (use one value for all family devices)
VITE_HOUSEHOLD_ID=kuleana
